[project]
name = "logalizer"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[project.scripts]
logalizer = "logalizer.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ["logalizer"]
